node_modules/
dist/
.next/
.turbo/
.cache/
coverage/
*.tsbuildinfo

# env & secrets
.env
.env.bak
.env.*.tmp
.env.local
.env.*.local
# Temporary credential files written by Google GitHub Actions authentication.
gha-creds-*.json

# local data
.local-gcs/
seed-data/voice/*.txt

# OS
.DS_Store

# logs
*.log

# assistant local workspace (FILES_DRIVER=local)
.workspace/

# Scratch git worktrees created for isolated agent runs. They are checkouts of
# this same repository, so committing them would nest a working copy inside it.
.claude/worktrees/

# Xcode user state
**/xcuserdata/
**/*.xcuserstate
.artifacts/
