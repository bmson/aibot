rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Server SDKs use customer service-account IAM. No browser/mobile database access.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
