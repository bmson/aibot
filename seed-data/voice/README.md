Drop writing samples here as .txt or .md files (gitignored).

- Separate multiple samples in one file with a `---` line.
- Filename prefixes pick the register: email-*.txt, email-pro-*.txt, sms-*.txt, chat-*.txt
- Then run: pnpm voice:ingest  (add --prod for the production DB)
