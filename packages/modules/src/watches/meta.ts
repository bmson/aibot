import type { ModuleMeta } from '../contract.js';

export const watchesMeta = {
  name: 'watches',
  title: 'Watches',
  summary: 'Durable owner-defined inbox and content watches that notify when they fire.',
  configKeys: [],
  billing: {},
} satisfies ModuleMeta;
