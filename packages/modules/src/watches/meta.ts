import { defineModuleMeta } from '../kit.js';

export const watchesMeta = defineModuleMeta({
  name: 'watches',
  title: 'Watches',
  summary: 'Durable owner-defined inbox and content watches that notify when they fire.',
  configKeys: [],
  billing: {},
});
